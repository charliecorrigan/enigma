def generate_offset(formatted_date, key)
  # square the formatted date
  last_4_digits = # take the last 4 digits of that number
  return last_4_digits
end

def format_date(date)
  return formatted_date
end

def generate_a_offset(last_4_digits)
  a_offset = # last_4_digits[0]
  return a_offset
end

def generate_b_offset(last_4_digits)
  b_offset = # last_4_digits[1]
  return b_offset
end

def generate_c_offset(last_4_digits)
  c_offset = # last_4_digits[2]
  return c_offset
end

def generate_d_offset(last_4_digits)
  d_offset = # last_4_digits[3]
  return d_offset
end
